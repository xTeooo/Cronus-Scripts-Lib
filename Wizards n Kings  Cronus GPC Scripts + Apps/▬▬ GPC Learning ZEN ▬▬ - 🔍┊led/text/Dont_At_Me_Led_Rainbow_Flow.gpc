// Made By Don't At Me (ME.)

int inc = 1, dec, color[3];

init{ color = 255; }

main { combo_run(rgb); }

combo rgb {
  wait(1);
  set_rgb(color, color[1], color[2]);
  color[dec] -= 1; color[inc] += 1;
  if(!color[dec]) { inc = (inc + 1) % 3; dec = (dec + 1) % 3; }
}