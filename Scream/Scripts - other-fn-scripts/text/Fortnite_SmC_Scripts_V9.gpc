



/* Este Scrip esta echo por Arturo Valdez para Fornite y especializado en Cronuz zen 





Fortnite 2022  V9 Capitulo 3 Temporada 1 
Arturo Valdez
Kito Roses
Gamvosaurio
Edgardo Ruiz    Programadores en videojuegos y Software 

=======================



  





*///--------------------------- 



int for9 = FALSE;// PUT TRUE TO Change Your shooting to L1 and R1


















///--------------------------- 


int Notify = TRUE; 
int for1 = TRUE; 
int for12 = 19; 
int for2 = 500; 
int for3 = 0; 
int for4 = TRUE; 
int library99;
int library34; 
int for5 = 15; 
int for6 = TRUE; 
int for7; 
int for8 = FALSE; 
int for10 = TRUE; 
int for11 = TRUE; 
int for13 = TRUE; 
int for14 = FALSE; 
int for16 = 130; 
int for17 = 41; 
define for18 = 4; 
define for19 = 5; 
define for20 = 17; 
define for21 = 18; 
int for22 = TRUE; 
int for23 = for21; 
int for24 = FALSE; 
int for25 = for19; 
int num, i, a; 
int button; 
int for26 = 150; 
int for27 = for21; 
int WAIT;
main 
	{ 
		if(for8) 
			if(get_val(PS4_L2) && get_val(PS4_R2)) combo_run(for28);
	} 
	combo for28 
		{ 
			set_val(PS4_R3,100); 
			wait(for17); 
			wait(for16); 
			set_val(PS4_R3,100); 
		} 
	main 
		{ 
			vm_tctrl(-9); 
			if(for22)
				{ 
					if(get_val(for23) && get_ptime(for23) > for26)
						{ 
							for24 = TRUE;
						}
				} 
			else if(!for22)
				{ 
					if(get_val(for23))
						{ 
							for24 = TRUE;
						}
				} 
			if (for24 == TRUE) 
				{ 
					if (get_val(for21)) 
						{ 
							for1 = TRUE;
						} 
					if (event_release(for18) || get_val(for25) || get_val(for20)) 
						{ 
							set_click(for27, 1, 0); for24 = FALSE;
						}
				} 
			if (a) 
				{ 
					if (i < num) 
						{ 
							if (!combo_running(for29)) 
								{ 
									combo_run(for29);
								}
						}
					else if (i > num) 
						{ 
							a = FALSE;
						}
				} 
					if(get_val(4))
						{ 
							set_val(4,100); 
						} 
					if(for9) 
						{ 
							swap(PS4_R1, PS4_R2); swap(PS4_L1, PS4_L2); 
						} 
					if(for13)
						{ 
							if(get_val(PS4_L2) && get_val(PS4_R2))
								{ 
									sensitivity(PS4_RX,0,for30);
									sensitivity(PS4_RY,0,for30); 
								} 
						} 
					if(get_val(PS4_L2) && event_press(PS4_RIGHT)) 
						{ 
							for8=!for8; if(for8)
								{ 
									combo_run(Flash_ON); 
								} else combo_run(Flash_OFF); 
						} 
					if(for4) 
						{ 
							if(get_val(7)> 80 && get_val(4)) 
								{
									library25 (180,40);
								} 
							if(event_release(7)) 
								{ 
									combo_stop(for4); 
								} 
							if (for11) 
								{ 
									if (get_val(PS4_L2)> 95)
										{ 
											combo_run(for31);
										} 
									if (abs(get_val(9)) > 21 || abs(get_val(10)) > 21) combo_stop(for31); 
								} 
							if (for6)
								{ 
									if (get_val(PS4_L2) && get_val(PS4_R2)) 
										{ 
											combo_run(for5); 
										} 
									if (abs(get_val(PS4_RY)) > 36 || abs(get_val(PS4_RX)) > 36) 
										{ 
											combo_stop (for5); 
										} 
								} 
							if(abs(get_val(PS4_LY)) < 5 && abs(get_val(PS4_LX)) < 5 )
								{ 
									set_val(PS4_LY,0); set_val(PS4_LX,0); 
								} 
							if(abs(get_val(PS4_RY)) < 5 && abs(get_val(PS4_RX)) < 5 )
								{ 
									set_val(PS4_RY,0); set_val(PS4_RX,0); 
								} 
							if (for10) 
								{ 
									if(get_val(PS4_L2) && get_val(PS4_R2)) 
										{ 
											for32 = 140; for33 = 0; combo_run(for34);
										} 
									else if (combo_running(for34)) 
										{ 
											combo_stop(for34);
										} 
								} 
							if(get_val(PS4_L2) && event_press(PS4_UP)) 
								{ 
									combo_run (vibrate); 
									for14=!for14; 
									if(for14)
										{ 	
											combo_run(Flash_ON); 
										} 
									else combo_run(Flash_OFF); 
								} 
							if(for1)
								{ 
									if(get_val(PS4_L2)> 95)
										{ 
											combo_run(for1);
										} 
									if(event_release(PS4_L2))
										{ 
											combo_stop(for1);
										} 
								} 
							if(for14) 
								{ 
									if(get_val(PS4_R2)) 
										{ 
											combo_run (for15); 
										}
								} 
							if(get_val(PS4_R2)>=1) set_val(PS4_R2, 100); 
							if(get_val(PS4_L2)>=1) set_val(PS4_L2, 100); 
							for36 = 500 / for35; for37 = for36 - 20; 
							if(for37 < 0) for37 = 0;
						} 
		} 
		combo vibrate
			{ 
				wait(300); 
				reset_rumble(); 
			} 
		combo for15 
			{ 
				set_val(PS4_R2, 100); 
				wait(for36); 
				set_val(PS4_R2, 0); 
				wait(for37); 
				set_val(PS4_R2, 0); 
			} 
		combo for31 
			{ 
				set_val(10, for12 * -1); 
				wait(10) set_val(9, for12); 
				wait(10) set_val(10, for12); 
				wait(10) set_val(9, for12 * -1); 
				wait(10) 
			} 
		combo for4 
			{ 
				set_val(7, 100); 
				wait(library99); 
				set_val(7, 0); 
				wait(library34); 
			} 
		combo for29 
			{ 
				wait(WAIT); 
				set_val(button, 100); 
				wait(28); set_val(button, 0); 
				wait(28); i = i + 1;
			} 
		combo for34 
			{ 
				set_val(PS4_L2, 100); 
				wait(for32); 
				set_val(PS4_L2, 0); 
				wait(for33);
			} 
		combo for1 
			{ 
				set_val(PS4_L2, 100); 
				wait(for2); 
				set_val(PS4_L2, 0); 
				wait(for3);
			} 
		int for30 = 95; 
		int for35 = 50; 
		int Col_ind; 
		int for36; 
		int for37; 
		int for32; 
		int for33; 
		combo for5 
			{ 
				if(get_val(PS4_L2)) 
					{ 
						for7 = get_val(10) + for5; 
							if(for7 > 100) for7 = 100; 
								set_val(10, for7); 
					} 
			} 
		combo Flash_ON 
			{ LED(0,0,2,0); 
				set_rumble(RUMBLE_A,100); 
				wait(400); reset_rumble(); reset_leds();
			} 
		combo Flash_OFF 
			{ 
				LED(0,2,0,0); 
				set_rumble(RUMBLE_B,100); 
				wait(200); 
				reset_rumble(); 
				LED(0,0,0,0); 
				wait(100); 
				set_rumble(RUMBLE_B,100); 
				LED(0,2,0,0); 
				wait(200); 
				reset_rumble(); 
				reset_leds();
			} 
		function set_click(B, Num, Wait) 
			{ 
				WAIT = Wait; 
				i = 0; 
				button = B; 
				num = Num; 
				a = TRUE;
			} 
		function LED(a,b,c,d) 
			{ 
				set_led(LED_1,a); 
				set_led(LED_2,b); 
				set_led(LED_3,c); 
				set_led(LED_4,d);
			} 
		function library25(H,R) 
			{ 
				library99 = H; library34 = R; combo_run(for4); 
			}